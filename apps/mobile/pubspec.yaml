name: nocta
description: "NOCTA — sleep identity app (feature-first Clean Architecture + Riverpod)."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management — tek desen (CLAUDE.md §3.1). BLoC/GetX/Provider karışımı yasak.
  flutter_riverpod: ^2.6.1
  # Navigasyon — tip güvenli route'lar (docs/04).
  go_router: ^14.6.2
  # HTTP — interim api client (üretilen Dart client Java-bloke, B-3; gelince değişecek).
  http: ^1.2.2
  # Güvenli token saklama (mikrofon ham verisi gibi hassas veri değil; oturum token'ları).
  flutter_secure_storage: ^9.2.2
  # i18n — CLAUDE.md §4: tüm kullanıcı metinleri arb'de, hard-code yasak.
  flutter_localizations:
    sdk: flutter
  intl: any
  just_audio: ^0.10.6
  # Alarmın ALARM ses kanalından çalması için (USAGE_ALARM): varsayılan USAGE_MEDIA
  # gece medyası kısık kullanıcıda alarmı SESSİZ bırakıyordu (emülatörde yakalandı).
  # just_audio zaten geçişli getiriyor; doğrudan import edildiği için açıkça bildirilir.
  audio_session: ^0.2.2
  share_plus: ^12.0.2
  record: ^7.1.1
  flutter_foreground_task: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Native ses grafı (#172) emülatörde gerçekten çalar mı — cihazda e2e doğrulama.
  # `flutter test` (CI) integration_test/'i koşmaz; yerel emülatörde açıkça koşulur.
  integration_test:
    sdk: flutter
  # Alarm testleri saatlerce süren geceyi milisaniyede koşturur: sahte zaman şart.
  # (flutter_test zaten geçişli olarak getiriyor ama doğrudan import edildiği için
  # açıkça bildirilir — depend_on_referenced_packages.)
  fake_async: ^1.3.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Uygulama kimliği: launcher ikonu + splash üretimi (varsayılan Flutter logosu
  # ve beyaz splash flaşı "yarım build" izlenimi veriyordu). Kaynak SVG'lerden
  # rasterlenen PNG'ler assets/icon/'da; generatörler mipmap/drawable üretir.
  flutter_launcher_icons: ^0.14.1
  flutter_native_splash: ^2.4.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # arb -> AppLocalizations kod üretimi (flutter tool; build_runner gerekmez).
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Inter — tema `fontFamily: 'Inter'` BİLDİRİYORDU ama font bundle EDİLMİYORDU:
  # uygulama sessizce Roboto'ya düşüyordu, yani tasarım niyeti hiç uygulanmadı.
  # OFL lisanslı (assets/fonts/LICENSE.txt), gömülü dağıtıma izin verir.
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-400.ttf
          weight: 400
        - asset: assets/fonts/Inter-600.ttf
          weight: 600

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# ---- Uygulama kimliği (Faz 0 cila) ----
# Launcher ikonu: crescent (gece/uyku) + brand-violet gradyan. Android-only
# (iOS kapsamda değil — Mac yok). Adaptive: fg=hilal, bg=gradyan.
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "assets/icon/icon_background.png"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

# Splash: dark marka arka planı (beyaz flaşı öldürür) + ortada hilal logo.
flutter_native_splash:
  color: "#0A0E1A"
  image: assets/icon/splash_logo.png
  android_12:
    color: "#0A0E1A"
    image: assets/icon/splash_logo.png
