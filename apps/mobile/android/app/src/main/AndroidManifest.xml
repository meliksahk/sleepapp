<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ██ INTERNET — BURADA OLMAK ZORUNDA, `src/debug/`de DEĞİL. ██

         Flutter şablonu INTERNET'i YALNIZCA `src/debug/` ve `src/profile/`
         manifestlerine koyar (hot reload için). `src/main/` yani KULLANICIYA GİDEN
         APK bu izin olmadan çıkıyordu ve iki şey birden ölüydü:

         1. **SES HİÇ ÇIKMIYORDU.** `BytesAudioSource` bir `StreamAudioSource`;
            just_audio bellekteki WAV'ı beslemek için yerel (localhost) bir proxy HTTP
            sunucusu açar. İzin yoksa bind edemez:
              SocketException: Failed to create server socket (errno = 1)
              #4 _ProxyHttpServer.start (just_audio.dart:2522)
            → mikser de (#138) alarm da (#146) release'de SESSİZDİ.
         2. API istemcisi: login, arketip, içerik, rapor — hepsi ölüydü.

         Beş iterasyon boyunca "cihazda doğruladım" dedim; hepsi DEBUG build'diydi ve
         debug manifesti izni gizlice ekliyordu. Ders: **"emülatörde duydum" ≠
         "kullanıcı duyar"**. Bunu bir daha kaçırmamak için `android_manifest_test.dart`
         bu satırı kilitliyor. -->
    <uses-permission android:name="android.permission.INTERNET" />

        <!-- Uyku takibi: mikrofon YALNIZCA dB zarfı ölçmek için; ham ses cihazdan çıkmaz
         ve hiçbir yerde saklanmaz (CLAUDE.md §6, bkz. SleepRecorder). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ARKA PLANDA GECE BOYU KAYIT (targetSdk=36 → Android 14+ kuralları).
         Android 14'ten beri mikrofonu arka planda kullanmak için foreground service
         ZORUNLU ve tipi "microphone" olmalı; aksi halde sistem kaydı ÖLDÜRÜR ve
         kullanıcı sabah BOŞ bir raporla uyanır. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Foreground service kalıcı bir bildirim GÖSTERMEK ZORUNDA (Android kuralı) ve
         Android 13+ bunun için ayrı izin ister. Bu aynı zamanda dürüstlük: kullanıcı
         mikrofonun açık olduğunu gece boyunca bildirimde GÖRÜR. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<application
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="nocta"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    
        <!-- Gece kaydı servisi. `microphone` tipi Android 14+ için ZORUNLU. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false" />
</application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
